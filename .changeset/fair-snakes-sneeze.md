---
'@alavida/agentpack': patch
---

Persist `skills dev` session ownership in `.agentpack/dev-session.json`, add stale-session recovery commands, and support recursive unlink for active dev roots. Also update shipped Intent skill metadata and sync-state so installed packages report staleness correctly.
