---
"@alavida/agentpack-release": patch
---

Unify authored skill package discovery and target resolution across `skills inspect`, `skills validate`, and `skills dev`, including first-class multi-skill package support. Also fix the bundled `intent` binary wrapper and prune removed authored skill entries from recorded build-state during no-arg validation.
