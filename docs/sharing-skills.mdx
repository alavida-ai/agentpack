---
title: "Sharing Skills Across Teams"
description: "Set up GitHub Packages to distribute versioned skills across teams and repositories."
---

Your frontend team's agents should use the same brand skills as your content team's agents. agentpack distributes skills as npm packages through GitHub Packages so every team gets the same versioned playbook.

## How it works

Skills are standard npm packages. Publishing goes through `npm publish`. Consuming teams install with `agentpack skills install`. GitHub Packages provides the distribution layer; agentpack handles skill materialization.

```text
Author repo                    Consumer repo
────────────                   ─────────────
SKILL.md + package.json        .npmrc (points @acme to GitHub Packages)
  │                              │
  ▼                              ▼
npm publish ──► GitHub Packages ──► agentpack skills install @acme/brand-copywriting
                               │
                               ▼
                        .claude/skills/brand-copywriting (materialized)
```

## Publisher setup

### 1. Configure the skill package

Your package's `package.json` must declare what it exports and point at GitHub Packages:

```json package.json
{
  "name": "@acme/brand-copywriting",
  "version": "1.2.0",
  "description": "Brand copywriting package.",
  "files": ["skills/"],
  "agentpack": {
    "skills": {
      "brand-copywriting": {
        "path": "skills/brand-copywriting/SKILL.md"
      }
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/acme-corp/knowledge-base.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
```

### 2. Configure `.npmrc` in the publishing repo

```ini .npmrc
@acme:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}
```

### 3. Authenticate

Create a GitHub token that can publish packages and set it as an environment variable:

```bash
export GITHUB_PACKAGES_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
```

### 4. Validate and publish

```bash
agentpack skills validate domains/brand/skills/copywriting
npm version patch -w domains/brand/skills/copywriting
npm publish -w domains/brand/skills/copywriting
```

## Consumer setup

### 1. Configure `.npmrc` in the consuming repo

```ini .npmrc
@acme:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}
always-auth=true
```

### 2. Authenticate

```bash
export GITHUB_PACKAGES_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
```

### 3. Install the skill package

```bash
agentpack skills install @acme/brand-copywriting
```

This:
1. Resolves the package and its transitive dependencies through npm
2. Materializes the exported skills into `.claude/skills/` and `.agents/skills/`
3. Records the install state in `.agentpack/install.json`

## CI tokens

For publishing in GitHub Actions:

```yaml .github/workflows/publish.yml
- name: Publish skill package
  env:
    GITHUB_PACKAGES_TOKEN: ${{ secrets.GITHUB_PACKAGES_TOKEN }}
  run: |
    cat << EOF > "$HOME/.npmrc"
    //npm.pkg.github.com/:_authToken=$GITHUB_PACKAGES_TOKEN
    EOF
    npm publish -w domains/brand/skills/copywriting
```

For consuming in CI:

```yaml .github/workflows/install-skills.yml
- name: Install skills
  env:
    GITHUB_PACKAGES_TOKEN: ${{ secrets.PACKAGES_READ_TOKEN }}
  run: |
    agentpack skills install @acme/brand-copywriting
```

## Verifying the setup

- `agentpack skills registry` confirms the scope, registry, and auth wiring
- `agentpack skills env` shows installed packages and materialized skills
- `agentpack skills outdated` shows newer available package versions
