---
'@alavida/agentpack': patch
---

Normalize the repository to a standard workspace package layout so `@alavida/agentpack` is published from `packages/agentpack` and the release workflow can use direct Changesets versioning and publishing.
